import { type Middleware } from 'moleculer';
import type { ValidationMiddlewareConfig } from '../../../type';
export declare const ValidateActionMiddleware: (config?: ValidationMiddlewareConfig) => Middleware;
